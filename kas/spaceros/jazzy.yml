header:
  version: 20
repos:
  meta-ros:
    url: "https://github.com/ros/meta-ros.git"
    branch: "scarthgap"
    path: "layers/meta-ros"
    layers:
      meta-ros-common:
      meta-ros2:
      meta-ros2-jazzy:

local_conf_header:
  ros2: |
    # ROS2 Jazzy Configuration
    ROS_DISTRO = "jazzy"
    ROS_OE_RELEASE_SERIES = "scarthgap"
   
  spaceros: |
    # Space-ROS base packages
    IMAGE_INSTALL:append = " packagegroup-ros-world-jazzy"